# DriveSmooth

A static, browser-based driving smoothness experiment. It uses `DeviceMotionEvent` on supported devices and automatically uses a 60 Hz simulator on localhost.

Serve this directory with any static web server, then open it in a browser. On a device, calibrate while the phone is still, then start a drive. Sessions are held in memory only.
